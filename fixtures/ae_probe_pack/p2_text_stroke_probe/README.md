# P2 Text Stroke Probe

Minimal AE pack for `TXT_DrawChar` stroke/fill merge tracing.

The pack renders a single Montserrat `W` with thick text stroke. Cases are kept
small so the existing `txt-drawchar` Frida profile can capture the native
`TXT_DrawChar` boundary without custom hook edits.

