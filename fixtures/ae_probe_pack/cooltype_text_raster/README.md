# CoolType Text Raster Probe

Minimal AE pack for tracing render-time text rasterization without building the
full conformance project. It creates `RAS_010` with large multiline
Montserrat text plus `RAS_020`, a single-glyph `W` case for focused
`TXT_DrawChar`/PF_World tracing.
