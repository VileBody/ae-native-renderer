# CoolType Text Raster Probe

Minimal AE pack for tracing render-time text rasterization without building the
full conformance project. It creates a single `RAS_010` comp with one large
Montserrat text layer and queues only that comp for PNG/TIFF output.
