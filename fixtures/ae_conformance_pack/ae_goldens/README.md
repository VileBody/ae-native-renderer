# AE Golden Output

Render PNG sequences here:

```text
png/<case_id>/<case_id>_[#####].png
```

The master reel preview can go here:

```text
preview/master_conformance_reel
```

Keep generated goldens out of normal source-control commits unless we explicitly
decide to check in a small subset.
