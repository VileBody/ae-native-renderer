# Case Directory

Case definitions currently live in the top-level `manifest.json` and are
implemented by `jsx/build_conformance_project.jsx`.

Future machine-generated scene JSON or per-case native render specs can live
here using the same `case_id` values.
