# AE Reference PNG Placeholders

Each subdirectory is a placeholder for exported AE PNG frames named
`frame_000000.png`, `frame_000001.png`, and so on. The repository ignores
general PNG outputs, so reference images can be generated locally without
accidentally committing proprietary renders.
