# P0-P5 Wide Pass - 2026-05-08

## Scope

One orchestrated wide pass across the current finish-line blocks:

- `P0`: refresh the master conformance dashboard.
- `P2`: run one final bounded `COV_O` row pass before leaving the text-raster microscope.
- `P1`: advance `M14` Turbulent Displace branch coverage from reverse evidence.
- `P4`: broaden glyph animator / expression selector telemetry and evaluator behavior.
- `P3`: broaden Glow / Shadow evidence and run focused effects gates.

The pass used subagents for independent work. Formula edits were accepted only
when backed by existing Ghidra/Frida/static evidence and checked against focused
native conformance. No open-ended metric fitting was used.

## Integrated Master Gate

Final integrated gate after accepted code changes:

```text
target/ae_agents/p0_master_gate_integrated_p1_p3_p4_20260508
```

Summary:

```text
cases:       19
accepted:    2
approximate: 16
tuning:      1
regressions: 0
missing:     0
```

Template status:

| Template | Status | Notes |
| --- | --- | --- |
| `template_4th` | `tuning` | Still blocked by `TXT_010` text/reveal/raster residual. |
| `impulse_2nd` | `approximate` | Broad P4 selector/evaluator substrate landed; visual formula parity still pending. |
| `scenes_3rd` | `approximate` | `EFF_060` accepted; `STK_030` is low-error approximate at `0.332818`. |

Accepted cases:

```text
TMP_020  primary=0.000000 max=0
EFF_060  primary=0.166828 max=2
```

Only tuning case:

```text
TXT_010  primary=12.531241 max=255
```

## P0 Result

Fresh standalone P0 run:

```text
target/ae_agents/p0_master_gate_refresh_20260508_230416
docs/phase_reports/P0_MASTER_GATE_REFRESH_20260508.md
```

Result: `accepted=2`, `approximate=16`, `tuning=1`, `regressions=0`.

The run made the big picture explicit: no global regression, but
`template_4th` remains in `tuning` because of `TXT_010`.

## P2 Result

Report:

```text
docs/phase_reports/P2_COVO_FINAL_ROW_PASS_20260508.md
target/ae_agents/p2_cov_o_final_row_pass_20260508
target/ae_agents/p2_covo_final_row_pass_gate_20260508
```

Decision: stop for now. The pass found no safe formula edit because the
available `COV_O` evidence lacks authoritative dynamic row-getter coverage rows.

Key row facts:

```text
COV_O TXT/ARE span compare:
AE merged ink rows: 113
native rows:        107
normalized extents: AE 75x71, native 75x71
normalized ink:     97 / 113
ink byte exact:     0 / 97

COV_O row getter:
rows: 0
status: instrumentation-incomplete
```

Focused text gate stayed unchanged:

```text
TXT_010  12.531241
TXT_020   6.410895
TXT_030   7.073936
TXT_040   3.671418
GPH_010   4.002211
```

Next P2 step is a real `COV_O` dense row-getter / hot byte capture, not another
native curve tweak.

## P1 Result

Report:

```text
docs/phase_reports/P1_M14_TURBULENT_PASS_20260508.md
target/ae_agents/p1_m14_turbulent_frac1d_pass_20260508
target/ae_agents/p1_m14_turbulent_frac1d_master_gate_20260508
```

Accepted code change: `M14` now implements the recovered `Frac1D` lookup branch
for public Turbulent displacement types `7`, `8`, and `9`.

Focused metrics:

```text
EFF_060             0.166828
STK_030             0.332818
STK_030_S00_BASE    0.000000
STK_030_S01_GEOM    0.310652
STK_030_S02_POSTER  0.310652
STK_030_S03_MINIMAX 0.310909
STK_030_S04_TURB    0.317022
```

Read: P1 is no longer blocked by the old Turbulent frame spike. Remaining M14
work is branch coverage: antialiasing footprint, resize-layer edge helpers,
cycle evolution, and nonzero seed.

## P4 Result

Report:

```text
docs/phase_reports/P4_TEXT_ANIMATOR_EXPRESSION_SWEEP_20260508.md
target/ae_agents/p4_text_animator_expression_sweep_20260508
```

Accepted code changes:

- Added text-engine helpers for range + bounce expression selector composition.
- Added telemetry structs for raw/clamped Amount, `textIndex`, `textTotal`,
  local delayed time, range weight, expression weight, and final weight.
- Updated generated bounce expression evaluator so pre-delay `else { value }`
  returns `ctx.value` when available and preserves the old zero fallback when it
  is absent.

Focused conformance stayed green:

```text
TXT_030  7.073936
TXT_040  3.671418
EXP_010  1.288731
GPH_010  4.002211
```

Read: this is a substrate/telemetry expansion, not visual parity lock yet.
Next P4 work should wire the new helper into render-core runtime and compare
against AE expression-selector amount telemetry.

## P3 Result

Report:

```text
docs/phase_reports/P3_GLOW_SHADOW_SWEEP_20260508.md
target/ae_agents/p3_glow_shadow_sweep_float_blur_20260508
target/visual_review/p3_glow_shadow_sweep_20260508
```

Accepted code change: Glow recursive Gaussian now keeps the horizontal and
vertical passes in float storage and quantizes once at the blur boundary.

Focused metrics:

```text
EFF_010  0.115046 -> 0.115046
EFF_020  1.628605 -> 1.629317
EFF_070  0.352847 -> 0.353011
STK_010  0.069822 -> 0.069822
STK_020  4.836876 -> 4.836876
```

Read: this was accepted as a small architecture/evidence alignment, not as a
metric win. The residual Glow blocker is still
`IR_CompositeWithBlendMode` premult/blend worker semantics.

## Verification

```text
cargo fmt --all --check
cargo test -p effects
cargo test -p text-engine
cargo test -p expression-engine
cargo test -p render-core text
cargo test -p render-cli all_manifest_cases_have_native_recipes
python3 scripts/run_master_conformance_gate.py --out target/ae_agents/p0_master_gate_integrated_p1_p3_p4_20260508
```

All passed. The integrated master gate has `regressions=0`.

## Next Recommended Order

1. `P2`: capture authoritative dynamic `COV_O` row-getter / hot coverage bytes.
2. `P4`: wire selector composition helper into render-core and add AE Amount telemetry refs.
3. `P3`: recover Glow composite worker semantics through Ghidra/Frida.
4. `P1`: run narrow M14 branch probes for AA footprint, resize-layer, cycle evolution, and seed.
5. `P0`: keep the integrated master gate as the release dashboard after each accepted math patch.
